# Storage bucket URL for showcase static assets.
# CI uses the STORAGE_URL repository variable (.github/workflows/visual.yml);
# ask a teammate or check the Vercel project env settings for the real value.
NEXT_PUBLIC_STORAGE_URL=https://example.com
