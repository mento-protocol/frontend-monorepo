## Codebase Patterns
- Feature flags use `z.enum(["true", "false"]).optional().default("false")` pattern in `env.mjs` with matching `runtimeEnv` entry
- Client components access env vars via `process.env.NEXT_PUBLIC_*` directly (Next.js inlines at build time)
- Navigation tabs are defined in `header.tsx` as a const array, active tab state lives in `atoms/navigation.ts`
- `turbo.json` `globalEnv` must include any new `NEXT_PUBLIC_*` vars for cache invalidation
- app.mento.org has no `check-types` script; run `tsc --noEmit` from its directory or `pnpm check-types` from root (covers packages only)
- `.env.local` is gitignored; `.env.example` is committed
- SDK version is managed via pnpm workspace catalog in `pnpm-workspace.yaml`; `packages/web3` uses `"catalog:"` reference; `apps/app.mento.org` has a direct version override
- SDK upgrades may introduce breaking type changes in existing hooks — always run `pnpm check-types` after upgrading
- Liquidity quote/transaction hooks use `deadline: bigint` (Unix timestamp) — transactions use `block.timestamp + BigInt(20 * 60)`, quotes use `Date.now()` based timestamps
- Features barrel is at `packages/web3/src/features/index.ts` — add new features alphabetically
- Swap uses `hooks/index.ts` barrel pattern; pools lists hooks individually in its `index.ts` — swap pattern is cleaner for borrow
- `BorrowService` constructor: `new BorrowService(publicClient: PublicClient, chainId: number)`
- Existing SDK factory is in `features/sdk.ts` with a simple cache by chainId; borrow sdk.ts follows same caching pattern
- Borrow component directory: `apps/app.mento.org/app/components/borrow/` — follows pools pattern with a top-level view component
- Borrow navigation atom at `components/borrow/atoms/borrow-navigation.ts` — uses `BorrowView` type from `@repo/web3`
- Imperative tx utils use `wagmi/actions` (`sendTransaction`, `estimateGas`, `waitForTransactionReceipt`); `Config` type from `"wagmi"`
- SDK `CallParams` strings must be cast: `to` → `Address`, `data` → `Hex`, `value` → `BigInt(value || 0)`
- Leverage math functions are pure (no contract calls); on-chain data (price, CR, trove data) must be fetched separately and passed in
- All leverage bigint values use 18-decimal fixed-point (1e18 = 1.0 or 100%); convert JS number leverage factors via `Math.round(factor * 1000) * 1e18 / 1000`

---

## 2026-03-01 - US-001
- Added `NEXT_PUBLIC_ENABLE_BORROW` feature flag to `env.mjs` (client schema + runtimeEnv)
- Added to `.env.example` (default "false") and `.env.local` (set to "true" for local dev)
- Added to `turbo.json` globalEnv
- Gated borrow tab in `header.tsx` — filters `allTabs` array based on flag, borrow tab hidden when flag is "false"
- Gated borrow section in `page.tsx` — added `shouldEnableBorrow` check to the borrow conditional render
- Files changed: `env.mjs`, `.env.example`, `.env.local`, `header.tsx`, `page.tsx`, `turbo.json`
- **Learnings for future iterations:**
  - The env validation uses `@t3-oss/env-nextjs` with Zod schemas — both `client` and `runtimeEnv` sections need updating
  - `AppTab` type in `atoms/navigation.ts` still includes "borrow" even when flag is off — the tab is just filtered from the UI
  - The git repo root is `/home/sol/projects/frontend-monorepo`, but `git status` CWD may drift — always `cd` to repo root for git ops
---

## 2026-03-01 - US-002
- Updated `@mento-protocol/mento-sdk` from `^3.0.0-beta.6` (catalog) / `^3.0.0-beta.17` (app) to `^3.0.0-beta.18`
- SDK 3.0.0-beta.18 includes `BorrowService` with full borrow transaction building API
- Fixed breaking changes in liquidity types introduced by the SDK upgrade:
  - `LiquidityOptions` now requires `deadline: bigint` field — added to `use-zap-in-quote.ts` and `use-zap-out-quote.ts`
  - `ZapInQuote.expectedLiquidity` → `estimatedMinLiquidity`
  - `ZapOutQuote.expectedTokenOut` → `estimatedMinTokenOut`
  - `ZapOutQuote.amountOutMinA/B` → `amountOutFromA/B`
- Updated downstream consumers in `add-liquidity-form.tsx` and `remove-liquidity-form.tsx`
- Files changed: `pnpm-workspace.yaml`, `apps/app.mento.org/package.json`, `pnpm-lock.yaml`, `use-zap-in-quote.ts`, `use-zap-out-quote.ts`, `add-liquidity-form.tsx`, `remove-liquidity-form.tsx`
- **Learnings for future iterations:**
  - SDK version is controlled in two places: workspace catalog (`pnpm-workspace.yaml`) and a direct override in `apps/app.mento.org/package.json`
  - The SDK beta.18 exports: `BorrowService`, `BorrowPosition`, `CallParams`, `SystemParams`, `LoanDetails`, `OpenTroveParams`, `AdjustTroveParams`, `InterestRateBracket`
  - When upgrading SDK, check for breaking changes in pool/swap/liquidity types too — the SDK covers multiple services
  - Existing transaction hooks already had `deadline` support; only quote hooks were missing it
---

## 2026-03-01 - US-003
- Created `packages/web3/src/features/borrow/types.ts` with frontend-specific borrow types
- Defined `DebtTokenConfig` interface (symbol, currencySymbol, currencyCode, locale)
- Defined `BorrowView` union type for navigation states (dashboard, open-trove, manage-trove, earn, redeem)
- Defined `StabilityPoolPosition` interface (deposit, collateralGain, debtTokenGain)
- Created `DEBT_TOKEN_CONFIGS` registry with GBPm config
- Re-exported SDK types: `BorrowPosition`, `LoanDetails`, `SystemParams`, `CallParams`, `OpenTroveParams`, `AdjustTroveParams`, `InterestRateBracket`, `TroveStatus`, `RiskLevel`
- Files changed: `packages/web3/src/features/borrow/types.ts` (new)
- **Learnings for future iterations:**
  - SDK borrow types are in `core/types/borrow.d.ts` — includes `BorrowPosition`, `LoanDetails`, `SystemParams`, `OpenTroveParams`, `AdjustTroveParams`, `InterestRateBracket`, `TroveStatus`, `RiskLevel`, `BorrowContractAddresses`
  - SDK transaction types (`CallParams`) are in `core/types/transaction.d.ts` — has `to`, `data`, `value` (all strings)
  - The pools feature uses the same `export type { CallParams } from "@mento-protocol/mento-sdk"` re-export pattern in its `types.ts`
  - `DEBT_TOKEN_CONFIGS` can be extended with additional stablecoins (e.g., USDm, EURm) as new branches are deployed
---

## 2026-03-01 - US-004
- Created borrow directory scaffold in `packages/web3/src/features/borrow/`
- Created `sdk.ts` — BorrowService factory with Map-based cache keyed by chainId
- Created barrel exports: `hooks/index.ts`, `atoms/index.ts`, `stability-pool/index.ts`, `tx-flows/index.ts`, `leverage/index.ts`
- Created `index.ts` — main barrel re-exporting from types.ts, sdk.ts, and all sub-modules
- Added `export * from "./borrow"` to `packages/web3/src/features/index.ts` (alphabetically placed)
- tsup build and pnpm check-types both pass
- Files changed: `sdk.ts` (new), `hooks/index.ts` (new), `atoms/index.ts` (new), `stability-pool/index.ts` (new), `tx-flows/index.ts` (new), `leverage/index.ts` (new), `index.ts` (new), `features/index.ts` (modified)
- **Learnings for future iterations:**
  - Features barrel in `features/index.ts` exports alphabetically — maintain this convention
  - Swap uses `hooks/index.ts` barrel; pools exports hooks individually — borrow follows swap pattern
  - `BorrowService` constructor takes `(publicClient: PublicClient, chainId: number)` from viem
  - Empty barrel files with just a comment are fine for tsup — they compile to empty exports
  - The existing SDK factory (`features/sdk.ts`) uses a plain object cache; borrow sdk.ts uses `Map` for cleaner semantics
---

## 2026-03-01 - US-005
- Created `apps/app.mento.org/app/components/borrow/borrow-view.tsx` — placeholder view matching pools-view layout pattern (top-decorations, bg-card, heading + description)
- Created `apps/app.mento.org/app/components/borrow/atoms/borrow-navigation.ts` — exports `borrowViewAtom` with `BorrowView` type from `@repo/web3`, initial value `'dashboard'`
- Updated `page.tsx` — imported `BorrowView` component and replaced "Coming soon" placeholder with `<BorrowView />`; borrow tab still gated behind `shouldEnableBorrow` feature flag
- Files changed: `borrow-view.tsx` (new), `borrow-navigation.ts` (new), `page.tsx` (modified)
- **Learnings for future iterations:**
  - `pools-view.tsx` is the best pattern reference for new tab views — uses `max-w-5xl` container with top-decorations
  - `page.tsx` renders tab content conditionally: `{activeTab === "X" && <View />}` — borrow adds feature flag guard too
  - app.mento.org `tsc --noEmit` must be run separately from `pnpm check-types` since the app has no check-types script
  - `BorrowView` type is exported from `@repo/web3` (borrow/types.ts) and can be imported in both app and web3 packages
---

## 2026-03-01 - US-006
- Created `packages/web3/src/features/borrow/hooks/use-borrow-service.ts` — hook returning cached BorrowService instance
- Uses `usePublicClient({ chainId })` and `useChainId()` from wagmi
- Uses `useMemo` to only recreate when publicClient or chainId changes
- Delegates to `getBorrowService()` factory in `sdk.ts` for Map-based caching
- Returns `BorrowService | null` (null when publicClient not ready)
- Updated `hooks/index.ts` barrel to export the hook
- Typecheck and tsup build both pass
- Files changed: `hooks/use-borrow-service.ts` (new), `hooks/index.ts` (modified)
- **Learnings for future iterations:**
  - Hook pattern: `useChainId()` + `usePublicClient({ chainId })` + `useMemo` is the standard wagmi service hook pattern in this codebase
  - The `getBorrowService` factory in `sdk.ts` caches by chainId only — the hook's `useMemo` handles publicClient changes
  - Hooks that return service instances follow the `useXxxService(): Service | null` convention
  - The existing pools/swap hooks import `usePublicClient` with `{ chainId }` parameter for chain-specific client
---

## 2026-03-01 - US-007
- Created `packages/web3/src/features/borrow/tx-flows/send-tx.ts` — bridge between SDK CallParams and wagmi sendTransaction
- `sendSdkTransaction(wagmiConfig, callParams, gasHeadroom?)` — estimates gas, applies 25% headroom buffer, sends via wagmi/actions
- `waitForTx(wagmiConfig, hash)` — wraps `waitForTransactionReceipt` from wagmi/actions
- Error normalization: user rejected, reverted (with reason extraction), insufficient funds, pass-through
- Updated `tx-flows/index.ts` barrel to export both functions
- Typecheck, tsup build, and app tsc all pass
- Files changed: `tx-flows/send-tx.ts` (new), `tx-flows/index.ts` (modified)
- **Learnings for future iterations:**
  - `wagmi/actions` provides imperative (non-hook) equivalents: `sendTransaction`, `estimateGas`, `waitForTransactionReceipt` — use these for utility functions, hooks for components
  - `Config` type is imported from `"wagmi"` (not `wagmi/actions` or `@wagmi/core`)
  - SDK `CallParams` has `to`, `data`, `value` as plain strings — must cast to `Address`/`Hex`/`BigInt` for wagmi
  - The existing `wagmi.ts` barrel re-exports from `"wagmi/actions"` — `sendTransaction` wasn't listed but can be imported directly in feature code
  - Gas estimation + headroom pattern: `estimateGas()` then `gas: estimate + ceil(estimate * headroom)`
---

## 2026-03-01 - US-008
- Extracted StabilityPool ABI from `/home/sol/projects/bold/frontend/app/src/abi/StabilityPool.ts`
- Created `packages/web3/src/features/borrow/stability-pool/abi.ts` — exported as `stabilityPoolAbi` typed const (`as const`)
- Updated `stability-pool/index.ts` barrel to re-export the ABI
- ABI includes all required functions: `provideToSP`, `withdrawFromSP`, `getDepositorCollGain`, `getDepositorYieldGain`, `getTotalBoldDeposits`, `getCompoundedBoldDeposit`
- tsup build, `pnpm check-types`, and app `tsc --noEmit` all pass
- Files changed: `stability-pool/abi.ts` (new), `stability-pool/index.ts` (modified)
- **Learnings for future iterations:**
  - The BOLD ABI source is auto-generated TypeScript at `bold/frontend/app/src/abi/StabilityPool.ts` — exported as `StabilityPool` (PascalCase), renamed to `stabilityPoolAbi` (camelCase) in our codebase
  - The ABI is a plain JSON array with `as const` — no imports needed, fully self-contained
  - StabilityPool key functions for Phase 1 earn/redeem: `provideToSP(amount, doClaim)`, `withdrawFromSP(amount, doClaim)`, `getCompoundedBoldDeposit(depositor)`, `getDepositorCollGain(depositor)`, `getDepositorYieldGain(depositor)`, `getTotalBoldDeposits()`
  - Additional useful read functions: `getDepositorYieldGainWithPending`, `getEntireBranchColl`, `getEntireBranchDebt`, `getYieldGainsOwed`, `getYieldGainsPending`
---

## 2026-03-01 - US-009
- Created `packages/web3/src/features/borrow/format.ts` with FX-aware currency formatting utilities
- `formatDebtAmount(amount, debtToken)` — converts 18-decimal bigint to locale-formatted currency string (e.g. £1,234.56)
- `formatCollateralAmount(amount)` — formats as "1,234.56 USDm"
- `formatPrice(price, debtToken)` — formats as "£0.79 per USDm"
- `formatInterestRate(rate)` — converts 18-decimal rate to percentage (e.g. "5.50%")
- `formatLtv(ltv)` — converts 18-decimal ratio to percentage with 1 decimal place (e.g. "72.3%")
- All functions handle null/undefined input gracefully, returning "—" placeholder
- Uses `Intl.NumberFormat` with locale from `DebtTokenConfig` for locale-aware formatting
- Added `export * from "./format"` to `borrow/index.ts` barrel (placed alphabetically after types)
- tsup build, `pnpm check-types`, and app `tsc --noEmit` all pass
- Files changed: `format.ts` (new), `index.ts` (modified)
- **Learnings for future iterations:**
  - `Intl.NumberFormat` with `style: "currency"` and `currency` code handles symbol placement and decimal separators per locale
  - Helper `bigintToNumber(value, decimals)` converts 18-decimal bigint to JS number — precision is sufficient for display formatting
  - The `10n ** 18n` constant (`ONE`) represents 100% or 1.0 in the SDK's fixed-point convention
  - All formatting functions accept `bigint | null | undefined` — consistent with React query/hook patterns where data may not be loaded yet
---

## 2026-03-01 - US-010
- Extracted leverage math from BOLD's `liquity-leverage.ts` into pure functions at `packages/web3/src/features/borrow/leverage/math.ts`
- Converted 4 async/contract-dependent functions to pure bigint functions:
  - `getOpenLeveragedTroveParams(collAmount, leverageFactor, price)` — computes flash loan and max net debt for opening leveraged troves
  - `getLeverUpTroveParams(currentCollAmount, currentCR, leverageFactor, price)` — computes params for increasing leverage
  - `getLeverDownTroveParams(currentCollAmount, currentCR, leverageFactor, price)` — computes params for decreasing leverage
  - `getCloseFlashLoanAmount(entireDebt, price, slippagePercent?)` — computes flash loan amount for closing from collateral
- Internal helpers: `collateralRatioToLeverageRatio`, `leverageFactorToBigint`
- Updated `leverage/index.ts` barrel to export all 4 public functions
- tsup build, `pnpm check-types`, and app `tsc --noEmit` all pass
- Files changed: `leverage/math.ts` (new), `leverage/index.ts` (modified)
- **Learnings for future iterations:**
  - BOLD's leverage functions are async because they read on-chain data (price, trove data, ICR) — the math itself is simple bigint arithmetic
  - `leverageRatioToCollateralRatio(cr) = cr * 1e18 / (cr - 1e18)` — converts ICR to leverage ratio for comparison
  - Leverage factor (JS number like 2.5) → bigint: `Math.round(factor * 1000) * 1e18 / 1000` — avoids floating-point precision issues
  - Slippage: lever-up uses 5% over (×105/100), lever-down uses 5% under (×95/100), close uses configurable percent
  - The `useCheckLeverageSlippage` hook from BOLD was NOT extracted — it's a React hook with contract calls, not pure math; Phase 5 will build its own hook using these pure functions
  - BOLD's `Dnum` type is `[bigint, number]` (value + decimals) — we eliminated it entirely since SDK conventions use 18-decimal bigint everywhere
---
