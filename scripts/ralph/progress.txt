## Codebase Patterns
- Feature flags use `z.enum(["true", "false"]).optional().default("false")` pattern in `env.mjs` with matching `runtimeEnv` entry
- app.mento.org has no `check-types` script; run `tsc --noEmit` from its directory or `pnpm check-types` from root (covers packages only)
- App-layer typecheck: `cd apps/app.mento.org && npx tsc --noEmit` (not covered by `pnpm check-types`)
- SDK version `^3.0.0-beta.18`; managed via pnpm workspace catalog in `pnpm-workspace.yaml`
- Features barrel is at `packages/web3/src/features/index.ts` — add new features alphabetically
- Commitlint rejects subjects starting with uppercase IDs like "US-006 - Create..." — move ID to end: `feat: add foo (US-006)`
- Trunk (linter) auto-formats Tailwind class order on commit — no need to manually sort utility classes
- `BorrowService` constructor: `new BorrowService(publicClient: PublicClient, chainId: number)`
- `useBorrowService()` returns `BorrowService | null` — always guard with `enabled: !!sdk` in useQuery hooks
- Borrow hooks barrel at `packages/web3/src/features/borrow/hooks/index.ts` — exports alphabetically
- Borrow component directory: `apps/app.mento.org/app/components/borrow/` — dashboard/, shared/, open-trove/, manage-trove/, earn/, atoms/
- `borrow-view.tsx` is the top-level router for the borrow tab — reads `borrowViewAtom` and renders sub-views
- `BorrowView` union discrimination: use `typeof view === "object"` for the `manage-trove` variant, string comparison for the rest
- Borrow navigation atom at `components/borrow/atoms/borrow-navigation.ts` — uses `BorrowView` type from `@repo/web3`
- Formatting utils in `features/borrow/format.ts`: `formatDebtAmount`, `formatCollateralAmount`, `formatPrice`, `formatInterestRate`, `formatLtv` — all accept `bigint | null | undefined`
- `DEBT_TOKEN_CONFIGS` in `features/borrow/types.ts` — typed as `Record<string, DebtTokenConfig>` so use `!` assertion when accessing by key
- `selectedDebtTokenAtom` holds a `DebtTokenConfig` object (not a symbol string) — use `debtToken.symbol` for hook calls
- `borrowViewAtom` lives in app layer (`components/borrow/atoms/borrow-navigation.ts`), NOT in packages/web3
- `borrowFlowAtom` in `packages/web3/src/features/borrow/atoms/flow-atoms.ts` — persisted to localStorage, auto-shows FlowDialog when not null
- `FlowDialog` is self-managing — just render `<FlowDialog />` in borrow-view, no props needed
- App components use `@repo/web3` dist — rebuild web3 package (`pnpm --filter @repo/web3 build`) after adding new exports before running app tsc
- `wagmiConfig` obtained via `useConfig()` from `wagmi`; `account`/`address` from `useAccount()` via `@repo/web3/wagmi`
- Bigint conversion: `parseUnits(inputString, 18)` for user input → bigint, `formatUnits(bigintValue, 18)` for display (from viem)
- Collateral token address: `getTokenAddress(chainId, "USDm" as TokenSymbol)` from `@mento-protocol/mento-sdk` — requires `TokenSymbol` cast
- Debt token address: `getTokenAddress(chainId, debtToken.symbol as TokenSymbol)` — same pattern as collateral
- ERC-20 balance pattern: `useReadContract({ address, abi: erc20Abi, functionName: "balanceOf", args: [address] })` — returns raw `bigint`
- `CoinInput` from @repo/ui already handles numeric validation (decimal only, no negatives) — no need for extra sanitization
- @repo/ui Card composable pattern: `Card` > `CardHeader` > `CardTitle` + `CardAction`, `CardContent` for body
- @repo/ui `Tabs` pattern: `Tabs` > `TabsList` > `TabsTrigger` + `TabsContent` (Radix-based)
- @repo/ui `Button` variants: `default`, `outline`, `destructive`, `ghost`, `link`
- @repo/ui `Badge` variants: `default`, `secondary`, `destructive`, `outline`
- @repo/ui `Skeleton` for loading states — animate-pulse, rounded-none, bg-accent
- Write hook pattern: `useMutation` + `executeFlow(wagmiConfig, setFlowAtom, flowId, operation, account, stepDefs)` + `onSuccess` invalidates + toast
- Available read hooks: `useSystemParams`, `useCollateralPrice`, `useLoanDetails`, `useDebtSuggestions`, `useInterestRateChartData`, `useRedemptionRisk`, `usePredictUpfrontFee`, `useNextOwnerIndex`, `useTroveData`, `useUserTroves`, `useBorrowAllowance`, `useBranchStats`, `useInterestRateBrackets`, `useSurplusCollateral`, `useStabilityPool`, `useStabilityPoolStats`
- Available write hooks: `useOpenTrove`, `useAdjustTrove`, `useCloseTrove`, `useAdjustInterestRate`, `useClaimCollateral`, `useSpDeposit`, `useSpWithdraw`
- `useSpDeposit` accepts `{ symbol, amount, doClaim, wagmiConfig, account }` — two-step: check debt token allowance for SP address → approve with maxUint256 → buildSpDeposit → send
- `useSpWithdraw` accepts `{ symbol, amount, doClaim, wagmiConfig, account }` — single-step: buildSpWithdraw → send (no approval needed)
- `useStabilityPool(symbol, address)` returns `{ deposit: bigint, collateralGain: bigint, debtTokenGain: bigint } | null`
- `useStabilityPoolStats(symbol)` returns `{ totalDeposits: bigint } | null`
- Claim-only rewards: call `spWithdraw.mutate({ symbol, amount: 0n, doClaim: true, ... })` — withdraw 0 triggers claim
- `doClaim` parameter on SP deposit/withdraw controls whether accumulated gains (collateral + yield) are claimed in the same tx
- Dashboard components in `dashboard/` — `borrow-dashboard.tsx`, `position-card.tsx`, `stability-card.tsx`
- `stability-card.tsx` already shows SP position summary and navigates to `"earn"` view on click
- Shared components in `shared/` — `debt-token-selector.tsx`, `flow-dialog.tsx`, `flow-step.tsx`, `risk-badge.tsx`, `trove-metrics.tsx`
- Wiring new views into `borrow-view.tsx` is trivial: replace placeholder `<div>` with actual component, add import

---

## 2026-03-01 - US-001
- Created `earn-view.tsx` — Stability Pool overview with pool stats, user position, and navigation
- Wired `EarnView` into `borrow-view.tsx` replacing the placeholder div
- Files changed:
  - `apps/app.mento.org/app/components/borrow/earn/earn-view.tsx` (new)
  - `apps/app.mento.org/app/components/borrow/borrow-view.tsx` (updated import + render)
- **Learnings for future iterations:**
  - `useStabilityPool(symbol)` gets account internally via `useAccount()` — no need to pass address
  - `useStabilityPoolStats(symbol)` returns `{ data: bigint | undefined }` directly (not wrapped in an object with `totalDeposits`)
  - Pool share math reuse from `stability-card.tsx`: `Number((deposit * 10000n) / total) / 100` for 2-decimal percentage
  - Trunk auto-formats Tailwind classes on commit — confirmed again, no manual sorting needed
---

## 2026-03-01 - US-002
- Created `deposit-form.tsx` — Stability Pool deposit form with balance display, max button, claim rewards checkbox, and submit logic
- Files changed:
  - `apps/app.mento.org/app/components/borrow/earn/deposit-form.tsx` (new)
- **Learnings for future iterations:**
  - `useConfig()` from `wagmi` (not `@repo/web3/wagmi`) provides the `wagmiConfig` needed for mutation hooks
  - Debt token address follows same pattern as collateral: `getTokenAddress(chainId, debtToken.symbol as TokenSymbol)`
  - `useState` initial value for `doClaim` must be derived from props at component mount — doesn't react to prop changes (acceptable for form UX)
  - Trunk auto-formats Tailwind class ordering in committed files — confirmed again
---

## 2026-03-01 - US-003
- Created `withdraw-form.tsx` — Stability Pool withdraw form with deposit balance display, max button, claim rewards checkbox, and submit logic
- Mirrors deposit form pattern but uses current deposit as max (not wallet balance) and calls `useSpWithdraw()` instead of `useSpDeposit()`
- Files changed:
  - `apps/app.mento.org/app/components/borrow/earn/withdraw-form.tsx` (new)
- **Learnings for future iterations:**
  - Withdraw form is structurally identical to deposit form — main differences: max source (deposit vs wallet balance), hook used, error message text
  - `useSpWithdraw` is single-step (no approval needed) unlike `useSpDeposit` (two-step with allowance check)
  - No need to import ERC-20 abi, chainId, or SDK token address utils for withdraw — only the deposit amount from props is needed
---

## 2026-03-01 - US-004
- Created `claim-rewards.tsx` — standalone claim rewards component that calls `spWithdraw` with `amount: 0n` and `doClaim: true`
- Component returns `null` when no rewards exist (hidden entirely per AC)
- Files changed:
  - `apps/app.mento.org/app/components/borrow/earn/claim-rewards.tsx` (new)
- **Learnings for future iterations:**
  - Claim-only is the simplest SP component — no input, no amount parsing, just a button + rewards display
  - Pattern reuse: rewards display block (collateral gain + yield gain) is identical across deposit-form, withdraw-form, and claim-rewards — could be extracted to a shared component in future
  - `ClaimRewards` takes only `collateralGain` and `debtTokenGain` props (no `deposit` needed unlike deposit/withdraw forms)
---
